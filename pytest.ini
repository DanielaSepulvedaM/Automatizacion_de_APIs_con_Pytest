#test configuration for pytest, indicando dónde están los tests y el path para importar módulos de src
[pytest]
testpaths = tests
pythonpath = .
markers =
    contract: tests de contrato/validacion  (sin pegarle al servidor)
    integration: tests que llaman a la Api (requieren servidor corriendo)

